[tool.poetry]
name = "client-a-finance-automation-poc"
version = "0.1.0"
description = "多平台订单自动合并工具 - PoC 版本"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.0.0"
openpyxl = "^3.1.0"
playwright = "4.18.0"
python-dotenv = "^1.0.0"
pyyaml = "^6.0"

[tool.poetry.dev-dependencies]
black = "^23.0.0"
flake8 = "^6.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
